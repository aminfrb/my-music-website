# URL of the Spidermelody GraphQL backend.
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql

# Optional: realtime (direct messages) WebSocket URL. When unset it is derived
# from NEXT_PUBLIC_API_URL (http→ws, /graphql→/ws), e.g. ws://localhost:4000/ws
# NEXT_PUBLIC_WS_URL=ws://localhost:4000/ws
